"use client";
/*
import { authClient } from "@/lib/auth-client";

export default function SignOut() {
  return (
    <button
      onClick={async () => {
        await authClient.signOut({
          fetchOptions: {
            onSuccess: () => {
              console.log("Signed out");
            },
          },
        });
      }}
      className="w-72 h-16 flex items-center justify-center gap-2 p-4 bg-cream text-background font-semibold rounded-md hover:bg-cream/75 hover:scale-105 transition ease-in-out duration-200"
    >
      <p>Sign Out</p>
    </button>
  );
}
*/
